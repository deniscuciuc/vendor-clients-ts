# @deniscuciuc/__VENDOR__

FILL IN: one sentence about which API this is and what the client can do.

```bash
pnpm add @deniscuciuc/__VENDOR__
```

## How to use it

```ts
import { create__Vendor__Client } from '@deniscuciuc/__VENDOR__'

const client = create__Vendor__Client({ apiKey })
const result = await client.something()

if (result.ok) use(result.value)
else if (result.error.kind === 'transport') retryLater()
```

## What is worth knowing

FILL IN. The rows of this table are what somebody installing the package a year from now trips
over.

| | |
| --- | --- |
| Authentication | |
| Limits and quotas | |
| Timeout | 15 seconds |
| API version | |

More about the source in [docs/vendors/__VENDOR__.md](../../docs/vendors/__VENDOR__.md).
