## What this changes

## Why

## Checklist

- [ ] `pnpm verify` is green
- [ ] New behaviour and bug fixes have tests; a fix comes with the test that failed without it
- [ ] A changeset is included if a published package changed (`pnpm changeset`)
- [ ] Breaking changes are called out — a removed or renamed export is a major
