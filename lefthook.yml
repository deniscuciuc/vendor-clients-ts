pre-commit:
  parallel: true
  commands:
    check:
      glob: '*.{ts,js,mjs,json,md}'
      run: pnpm biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
